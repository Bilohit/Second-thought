.claude/
.worktrees/
node_modules/
target/
dist/
__pycache__/
*.pyc
.venv/
venv/
logs/
*.log
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
Thumbs.db
.superpowers/
CLAUDE.md
SKILLS.md
docs/
*.md
!README.md
.gitignore
mobile/
omni_capture/.mobile_sync_state.json

# OAuth material — never commit
omni_capture/client_secret.json
omni_capture/.drive_token.json

# Local config holds live X-Omni-Secret + LAN key once the app runs — never commit.
# Ship config.toml.example instead; the app writes a real config.toml on first launch.
omni_capture/config.toml
coverage/
omni_capture/.coverage
