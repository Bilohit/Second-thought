# Omni-Capture dependencies
# Install with: pip install -r requirements.txt

# ── Core pipeline ─────────────────────────────────────────────────────────────
pyperclip>=1.8.2                 # clipboard read/write
readability-lxml>=0.8.1          # web article extraction
youtube-transcript-api>=1.0,<2.0 # YouTube captions

# ── LLM / structured output ───────────────────────────────────────────────────
openai>=1.30.0                   # Ollama's OpenAI-compatible client
instructor>=1.3.0                # Pydantic-enforced LLM output + retry logic
pydantic>=2.7.0                  # data models & validation

# ── Config ────────────────────────────────────────────────────────────────────
# tomllib is stdlib in Python 3.11+.  For Python 3.10 and below:
tomli>=2.0.1; python_version < "3.11"
tomlkit>=0.12.0                  # used by server.py for comment/order-preserving config writes

# ── GUI server (FastAPI bridge) ───────────────────────────────────────────────
fastapi>=0.111.0                 # REST + SSE server for the Tauri GUI
uvicorn[standard]>=0.30.0        # ASGI server; [standard] pulls in websockets + httptools

# ── Image clipboard support ───────────────────────────────────────────────────
Pillow>=10.0.0                   # ImageGrab for clipboard images (Win/macOS)

# ── Audio transcription (local Whisper) ──────────────────────────────────────
openai-whisper>=20231117         # local Whisper transcription
# torch is a transitive dep of whisper — GPU users should install the
# CUDA build separately: pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── Desktop notifications ─────────────────────────────────────────────────────
plyer>=2.1.0                     # cross-platform fallback (macOS/Linux/Windows)
# Windows users may prefer the native toast:
# win10toast>=0.9; sys_platform == "win32"

# ── OCR (optional, gated behind [ocr] enabled=true in config.toml) ───────────
rapidocr-onnxruntime>=1.2.3      # CPU-only OCR pass for verbatim text/code in image captures

# ── Concurrency ───────────────────────────────────────────────────────────────
filelock                         # cross-platform file locks (dedup-index + merge-append RMW cycles)

# ── Mobile sync / pairing (Drive hub) ────────────────────────────────────────
google-api-python-client>=2.100.0   # Drive v3 REST client
google-auth-oauthlib>=1.2.0         # installed-app OAuth flow + token refresh

# ── LAN accelerator transport ─────────────────────────────────────────────────
pynacl>=1.5.0                       # app-layer secretbox encryption for /lan/* endpoints
