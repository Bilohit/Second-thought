# s109 GUI Fixes (P1-5, P1-6) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix two desktop pill-menu findings from the s109 `/flow-review fast` run: the capsule/radial menu never traps keyboard focus (P1-5), and the Drive OAuth consent screen's "Stop waiting" button is a dead control (P1-6, user picked option C — auto-timeout, single "Cancel" button, no on-screen countdown copy).

**Architecture:** P1-5 is a pure keyboard-behavior fix inside the existing `RadialMenu.tsx`/`CapsuleMenu.tsx` open/close lifecycle — no new component. P1-6 replaces the two-button dead-end in `SyncWizard.tsx` with a single button plus a real timeout that auto-resolves back to the sync-off state.

**Tech Stack:** TypeScript, React 18 (hooks only), Vitest.

## Global Constraints

- `gui/`: `strict` TS with `noUnusedLocals`/`noUnusedParameters`/`noFallthroughCasesInSwitch` — must satisfy, never suppress (repo CLAUDE.md).
- No new dependency. No new abstraction/component for a single call site.
- `npm run build` (tsc + vite build) MUST pass before any GUI commit (repo CLAUDE.md).
- `npm test` (vitest) must stay green; add a sibling test for any new pure logic.
- Icons: inline SVG only from `gui/src/components/PillMenu/icons.tsx` — never emoji, no new one-off copies.

---

### Task 1: Trap keyboard focus in the pill menu (P1-5)

**Files:**
- Modify: `Second Thought/gui/src/components/PillMenu/RadialMenu.tsx` (focus-trap effect, near the existing focus-management `useEffect`s at lines 94 and 107)
- Modify: `Second Thought/gui/src/components/PillMenu/CapsuleMenu.tsx` (same trap — d02's P2 finding notes the two menus are keyboard-inconsistent; fix both the same way)
- Test: sibling `*.test.ts`/`*.test.tsx` if the trap logic can be extracted as pure (e.g. a `getNextFocusIndex(current, count, direction)` helper) — extract it to `gui/src/lib/` only if it is genuinely reusable/pure; otherwise a component-level interaction test is acceptable per this repo's "no abstraction for one call site" rule.

**Interfaces:**
- Consumes: the menu's existing open/close state and `itemRefs` ref array (already present, see `RadialMenu.tsx:110,118`).
- Produces: on open, focus lands on the first focusable item (already partially true per line 110 — verify it still holds after the fix). On `Tab` from the last item, focus cycles to the first (not out to `BODY`). On `Shift+Tab` from the first item, focus cycles to the last. On `Escape`, the menu closes AND focus returns to the menu's trigger element (the pill/FAB button that opened it) — read how the trigger ref is currently passed/stored before adding this; if no reference to the trigger exists yet, thread one in via a new prop rather than querying the DOM.

- [ ] **Step 1: Read current behavior**

Read `RadialMenu.tsx` in full (it's short) and `CapsuleMenu.tsx`'s `onKeyDown` handler at line 255. Confirm via the desktop app (`.\launch.ps1`, open the pill menu, press Tab repeatedly) that focus currently escapes to `BODY` — this reproduces the s109 finding (measured via `document.activeElement`, not eyeballed, per `FACTS.md`'s "measure, never look" rule).

- [ ] **Step 2: Write the failing test**

Add a test (in whichever file already covers `RadialMenu`/`CapsuleMenu` interaction, or a new `PillMenu.focustrap.test.tsx` if none exists) that renders the menu open, simulates `Tab` past the last focusable item, and asserts `document.activeElement` is the FIRST item's element, not `document.body`. Mirror the existing test file's render/mock setup (check for an existing `RadialMenu.test.tsx` first — reuse its harness rather than inventing a new one).

- [ ] **Step 3: Run it to verify it fails**

Run: `npm test` (from `gui/`) — the new test should fail, `document.activeElement` reads `BODY`.

- [ ] **Step 4: Implement the trap**

Add a `keydown` handler (or extend the existing one) that intercepts `Tab`/`Shift+Tab` when focus is on the last/first item respectively, calls `.preventDefault()`, and moves focus via the existing `itemRefs` array — same mechanism the arrow-key navigation at `RadialMenu.tsx:118` already uses (reuse `itemRefs.current[...]?.focus()`, do not invent a second focus-management path). Add an `Escape` handler that closes the menu and restores focus to the trigger.

- [ ] **Step 5: Run tests, verify pass**

Run: `npm test` — new test passes, no regression in existing PillMenu tests.

- [ ] **Step 6: Manual QA (native window, not CDP)**

Per repo CLAUDE.md: run `.\launch.ps1`, open the pill menu, Tab through every item with the keyboard, confirm it never exits to `BODY`, confirm Escape closes and returns focus to the pill. Do this for BOTH RadialMenu and CapsuleMenu (open each mode).

- [ ] **Step 7: Full gate + commit**

Run: `npm run build` (must pass) and `npm test`.
```bash
git add gui/src/components/PillMenu/RadialMenu.tsx gui/src/components/PillMenu/CapsuleMenu.tsx gui/src/components/PillMenu/*.test.tsx
git commit -m "fix: trap keyboard focus inside the pill menu (P1-5)"
```

---

### Task 2: Fix the dead "Stop waiting" control on the Drive consent screen (P1-6, option C)

**Files:**
- Modify: `Second Thought/gui/src/components/Sync/SyncWizard.tsx` (button at line 189, its dead handler, and the "waiting for consent" copy at line 244)
- Test: sibling test file for `SyncWizard` if one exists (check before creating a new one) covering the new timeout-then-cancel behavior with fake timers.

**Interfaces:**
- Consumes: the existing `connecting`/`drive?.connecting` state (line 244) that currently renders "waiting for consent"; the existing "Skip for now" handler at line 320 (the ONLY currently-working escape) — the fix reuses that exact cancel path, it does not invent a new one.
- Produces: a single `Cancel` button, wired to the same handler "Skip for now" already calls. A real wait ceiling (a timer, suggested 60s — confirm against any existing timeout constant in `syncSetup.ts` before inventing a new number) that automatically fires the same cancel path if consent hasn't resolved.

- [ ] **Step 1: Read current behavior**

Read `SyncWizard.tsx` lines 150-330 in full: the "Stop waiting" button's current `onClick` (confirm it truly no-ops — trace where its handler is defined), the "waiting for consent" copy at line 244, and "Skip for now"'s handler at line 320 (this is the one proven-working escape per s109's arbitration notes).

- [ ] **Step 2: Write the failing test**

Add a test asserting: given `connecting: true` state held for longer than the timeout constant, the wizard automatically calls the same cancel function "Skip for now" calls (spy on it), without any user click. Use `vi.useFakeTimers()` / `vi.advanceTimersByTime(...)` — do not use a real `setTimeout` wait in the test.

- [ ] **Step 3: Run it to verify it fails**

Run: `npm test` — new test fails, no timeout exists yet.

- [ ] **Step 4: Remove the dead button, add the timer**

Delete the "Stop waiting" button block (line ~189) and its dead handler. Rename "Skip for now" to "Cancel" ONLY in this waiting-for-consent state (confirm via reading the surrounding conditional whether "Skip for now" is reused in a different, non-waiting state elsewhere in the same file — if so, keep that label there and introduce the "Cancel" label only for this state's render branch, do not rename the shared string globally). Add a `setTimeout`/timer effect that, once the waiting-for-consent state has been active for the chosen ceiling, calls the same cancel handler. No new countdown copy — the waiting-for-consent text itself does not change to mention the timeout (per user's explicit pick).

- [ ] **Step 5: Run tests, verify pass**

Run: `npm test` — passes.

- [ ] **Step 6: Manual QA**

`.\launch.ps1`, Settings ▸ Sync ▸ start the Drive wizard, reach the waiting-for-consent screen, confirm only one button ("Cancel") renders, confirm clicking it returns to sync-off immediately, and (if patient) confirm the auto-timeout fires without a click after the chosen ceiling.

- [ ] **Step 7: Full gate + commit**

Run: `npm run build` and `npm test`.
```bash
git add gui/src/components/Sync/SyncWizard.tsx gui/src/components/Sync/*.test.ts*
git commit -m "fix: replace the dead Stop-waiting control with a single Cancel + auto-timeout (P1-6)"
```

---

### Task 3: Three one-off inline SVGs duplicating exported icons at the wrong stroke weight (P2)

**Files:**
- Modify: `Second Thought/gui/src/components/LookPanel.tsx:391-397` and `:337-340`
- Modify: `Second Thought/gui/src/components/FullWindow/FullWindow.tsx:282-289`

**Interfaces:** Consumes the shared icon exports from `gui/src/components/PillMenu/icons.tsx` (`MenuIcon`/`RefreshIcon`/`ChatIcon`/`MicIcon`, `stroke=currentColor` at ~1.7 weight, 24-grid) — this repo's hard rule already forbids new one-off inline SVG copies when an export exists.

- [ ] **Step 1: Read all three inline SVGs**

Read `LookPanel.tsx:337-397` and `FullWindow.tsx:282-289`. For each, identify which icon it's visually duplicating and confirm the exported equivalent exists in `icons.tsx` (add a note if one genuinely doesn't exist yet — that would need a new export, not a bigger fix).

- [ ] **Step 2: Replace each inline `<svg>` with the shared icon component**

Swap each of the three inline SVG blocks for the matching import from `icons.tsx`, passing through whatever `size`/`className`/color props the surrounding JSX already threads to other icons in the same file (match the existing call pattern, don't invent new props).

- [ ] **Step 3: Run gates**

`npm run build`, `npm test`.

- [ ] **Step 4: Visual check**

`.\launch.ps1`, open the Look panel and the Full Window view, confirm the three icons render at the same stroke weight as every other icon on screen (a visual diff, not just "it still renders").

- [ ] **Step 5: Commit**

```bash
git add gui/src/components/LookPanel.tsx gui/src/components/FullWindow/FullWindow.tsx
git commit -m "fix: replace 3 one-off inline SVGs with the shared icon exports (P2)"
```

---

## Acceptance criteria

- [ ] Tab from the pill menu's last item lands on the first item, never on `BODY` — verified by a passing test AND a manual `.\launch.ps1` check, both RadialMenu and CapsuleMenu.
- [ ] Escape closes the menu and returns focus to its trigger.
- [ ] The Drive consent waiting screen shows exactly one button ("Cancel"), and it reaches sync-off state on click.
- [ ] All three one-off inline SVGs (LookPanel ×2, FullWindow ×1) are replaced with shared icon exports, visually matching stroke weight.
- [ ] The waiting screen also reaches sync-off automatically after the chosen timeout with zero clicks (fake-timer test + optional manual patience check).
- [ ] `npm run build` and `npm test` both green, re-run and read by you, not quoted from a subagent.
